using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestCode003T : MonoBehaviour
{
    //[議題]お米同士が重ならないように留意する点について

    //案1、カーソルにお米と同じ当たり判定を持ったオブジェクトを追従させ、その当たり判定がオブジェクトを取得しなければ生成という手法をとる

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
